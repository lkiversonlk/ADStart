//
   Created by kliu on 20/06/2015.

extends ../layout

block content
    link(rel='stylesheet', href='/stylesheets/user.css')
    section.header
        div.navbar.navbar-default(style="margin-bottom:0px")
            div.container(style="height:3em;text-align:center;")
                img(src="/images/title.png", style="height:100%")

    section.container-fluid.user_info(style="padding-top:10px; padding-bottom:10px")
        div.row
            div(style="width:30px; float:left; margin-left:10px;")
                img(src=user.iconUrl, style="width:100%")
            div(style="float:left; margin-left:10%;line-height:30px")
                | #{user.name}

    section.container
        div.row
            | 总计

    section.panel.panel-default.user_stat_panel.container
        div.row.panel-body
            div.col-md-4.col-xs-4
                img(src=user.iconUrl, style="height: 100%; width: 100%")
            div.col-md-8.col-xs-8.stat
                p
                    | 总带盐 #{user.total_delegations}
                p
                    | 通过 #{user.passed_delegations}   未通过 #{user.denied_delegations} 审核中 #{user.ongoing_delegations}
                p
                    | 平均带盐点击量 20%

    section.delegations
        div.container
            div.row
                span.glyphicon.glyphicon-globe
                | 我的带盐
        ul#delegation-list.list-group
            each delegation in delegations
                li
                    a.kliu-list-item(href="/delegations/" + delegation._id)
                        div.left(style="text-align:center; width:60px; height:60px")
                            img(src="/brands/" + delegation.brand +"/logo.png", style="height:100%")

                        div.left(style=" margin:5px 15px")
                            div
                                | #{delegation.brand_name}
                            div
                                | #{delegation.product_name}

                        div.right(style="line-height:50px")
                            case delegation.status
                                when "0"
                                    span.label.label-default
                                           | 审核中
                                when "1"
                                    span.label.label-success
                                           | 通过
                                when "2"
                                    span.label.label-danger
                                           | 未通过
                        div.right(style="line-height:25px; margin:5px")
                            div
                                | #{delegation.template_name}
                            div
                                | #{delegation.creationTime}





